---
phase: 02-trait-type-system
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - go/pkg/traits/traitset.go
  - go/pkg/traits/resolve.go
  - go/pkg/traits/query.go
  - go/pkg/traits/traitset_test.go
  - go/pkg/traits/resolve_test.go
  - go/pkg/traits/query_test.go
autonomous: true
requirements: [TRAIT-02, TRAIT-03]
must_haves:
  truths:
    - "Traits from multiple sources combine additively at the merge site with per-axis caps preventing stacking blowup (TRAIT-02)"
    - "The query API HasTrait/HasCapability/GetModifier/ResolveImmunity/HooksFor is callable from any package and unit-tested (TRAIT-03)"
    - "ResolveImmunity maps the summed/clamped magnitude back to a tri-state vocabulary (Immune > Resist > Vuln > Normal) WITHOUT importing pkg/combat"
    - "HasCapability is O(1) zero-allocation per query (SC#4) proven by testing.AllocsPerRun == 0"
  artifacts:
    - path: "go/pkg/traits/traitset.go"
      provides: "TraitSet per-kind slices + Merge/Compose + resolved caches"
      contains: "type TraitSet struct"
    - path: "go/pkg/traits/resolve.go"
      provides: "Resolve() — slices -> CapBits + clamped RIS sums + stat-mod sums"
      contains: "func (ts *TraitSet) Resolve()"
    - path: "go/pkg/traits/query.go"
      provides: "Query API: HasTrait/HasCapability/GetModifier/ResolveImmunity/HooksFor"
      contains: "func (ts *TraitSet) HasCapability"
    - path: "go/pkg/traits/query_test.go"
      provides: "Query API tests incl. zero-alloc HasCapability"
      contains: "AllocsPerRun"
  key_links:
    - from: "go/pkg/traits/resolve.go"
      to: "go/pkg/traits/capability.go"
      via: "intern Capability.Key -> CapBits.Set"
      pattern: "internCapability|CapBits"
    - from: "go/pkg/traits/query.go"
      to: "Resolve() caches"
      via: "ResolveImmunity reads clamped per-axis sum"
      pattern: "ResolveImmunity"
---

<objective>
Build the composition + query layer of `go/pkg/traits/` on top of Plan 01's structs and `CapBits`: the `TraitSet` value with per-kind slices and deterministic merge, `Resolve()` that produces the cached bitset + clamped RIS sums + stat-mod sums, and the five-method query API. Includes the parity-critical numeric->tri-state `ResolveImmunity` bridge and the SC#4 zero-alloc `HasCapability` test.

Purpose: Satisfies TRAIT-02 (additive composition with per-axis caps) and TRAIT-03 (the callable query API), and proves SC#4 (O(1) zero-alloc HasCapability). The `ResolveImmunity` output mirrors `combat.ImmunityResult` semantics exactly without importing `pkg/combat` (avoids a future import cycle, D-07).

Output: `traitset.go`, `resolve.go`, `query.go` + their tests — a complete, tested trait query API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trait-type-system/02-CONTEXT.md
@.planning/phases/02-trait-type-system/02-PATTERNS.md
@.planning/phases/02-trait-type-system/02-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 (go/pkg/traits/) — use directly, do not re-derive. -->

From go/pkg/traits/traits.go (Plan 01):
```go
type TraitKind int   // KindVulnerability, KindResistance, KindImmunity, KindModifier, KindCapability, KindHook
type HookEvent int   // OnBeforeDamage, OnAfterDamage, OnDeath, OnSpellCast, OnLevelUp
type Vulnerability struct { DamageType types.DamageType; Magnitude int }
type Resistance    struct { DamageType types.DamageType; Magnitude int }
type Immunity      struct { DamageType types.DamageType; Magnitude int }
type StatModifier  struct { Stat types.Stat; Delta int }
type Capability    struct { Key string }
type BehaviorHook  struct { Event HookEvent; Script string }
```

From go/pkg/traits/capability.go (Plan 01):
```go
type CapBits [4]uint64
func (b CapBits) Has(bit int) bool   // O(1), zero-alloc
func (b *CapBits) Set(bit int)
func internCapability(key string) (int, bool)   // alloc bit on first sight; bool=ok (false on >256 overflow)
func lookupCapability(key string) (int, bool)    // non-allocating read of already-registered bit
```

From go/pkg/types (D-02): `type DamageType int`; `type Stat = int` (alias); `const MaxStats = 5`.

The tri-state contract to MIRROR (do NOT import pkg/combat — cycle risk). From go/pkg/combat/combat.go:308-316:
```go
type ImmunityResult int
const ( ImmNormal ImmunityResult = iota; ImmImmune; ImmResistant; ImmVulnerable )
```
Precedence: Immune > Resist > Vuln > Normal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TraitSet composition value + deterministic Merge/Compose (traitset.go)</name>
  <files>go/pkg/traits/traitset.go, go/pkg/traits/traitset_test.go</files>
  <behavior>
    - Compose(a, b) yields a TraitSet whose per-kind slices are a's then b's elements, in that source order (deterministic, race -> class -> ...)
    - Compose of N sets preserves left-to-right source order across every kind slice
    - An empty/zero TraitSet composes cleanly (no nil-panic) and Merge into an empty set is identity
  </behavior>
  <read_first>
    - go/pkg/types/races.go:4-13 — struct-of-fields + trailing inline doc-comment style
    - go/pkg/combat/combat.go:33-48 — "struct holds the parts, methods operate on it" convention
    - go/pkg/traits/traits.go (Plan 01) — the six trait structs being held in slices
    - .planning/phases/02-trait-type-system/02-PATTERNS.md — "traitset.go" section (D-04 per-kind slices)
    - ./CLAUDE.md — Conventions
  </read_first>
  <action>
    Create `go/pkg/traits/traitset.go` in `package traits`, importing `"rotmud/pkg/types"`. Define `type TraitSet struct` with the six exported per-kind homogeneous slices (D-04 — NO `[]Trait` interface, NO type assertions): `Vulnerabilities []Vulnerability`, `Resistances []Resistance`, `Immunities []Immunity`, `Modifiers []StatModifier`, `Capabilities []Capability`, `Hooks []BehaviorHook`. Add unexported resolved-cache fields populated by `Resolve()` (Plan 02 Task 2): `caps CapBits`, `risSum map[types.DamageType]int`, `modSum [types.MaxStats]int`, and a `resolved bool` guard.

    Provide deterministic composition: `func Compose(sets ...TraitSet) TraitSet` appends each source set's per-kind slices in argument order (race -> class -> skill -> ... ), and a `func (ts *TraitSet) Merge(other TraitSet)` that appends `other`'s slices onto `ts` in place. Source order MUST be preserved exactly so `Resolve()` and `HooksFor` output is reproducible (merge determinism, Claude's Discretion). Composition only concatenates slices here — summing/clamping/interning happens in `Resolve()`, not at merge time. Do not touch the cache fields in Merge/Compose (a fresh Compose result is unresolved).
  </action>
  <verify>
    <automated>cd go && go test ./pkg/traits/ -run 'TestCompose|TestMerge|TestTraitSet' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go/pkg/traits/traitset.go` contains `type TraitSet struct` with the six exported slice fields named `Vulnerabilities, Resistances, Immunities, Modifiers, Capabilities, Hooks`
    - Unexported cache fields `caps CapBits`, `risSum map[types.DamageType]int`, `modSum [MaxStats]int` (or `[types.MaxStats]int`) exist for Resolve to populate
    - `func Compose(sets ...TraitSet) TraitSet` and `func (ts *TraitSet) Merge(other TraitSet)` preserve left-to-right source order in every kind slice (proven by test)
    - `cd go && go build ./pkg/traits/` exits 0
  </acceptance_criteria>
  <done>TraitSet with six per-kind slices + cache fields; Merge/Compose append deterministically in source order; tests for order preservation pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve() — bitset + clamped RIS sums + stat-mod sums (resolve.go)</name>
  <files>go/pkg/traits/resolve.go, go/pkg/traits/resolve_test.go</files>
  <behavior>
    - Two Vulnerabilities of magnitude 60 each on DamFire sum to 120, then clamp to +CAP (100) — proving per-axis cap prevents stacking blowup (TRAIT-02)
    - Resistance(+40) + Vulnerability(-30) on the same axis net to +10 (Resistances add positive, Vulnerabilities add negative magnitude into the per-axis sum)
    - Two StatModifiers on StatStr (+3, +2) sum to +5 in modSum, clamped to the per-stat cap
    - Resolve interns each Capability.Key and ORs its bit into caps; resolving twice is idempotent (stable output)
  </behavior>
  <read_first>
    - go/pkg/combat/combat.go:308-373 — CheckImmune precedence + the resist=2/3 / vuln=3/2 / immune=zero damage semantics being targeted (parity context; do NOT import)
    - go/pkg/types/constants.go:289-300 — table-walk transform style
    - go/pkg/traits/capability.go (Plan 01) — internCapability + CapBits.Set
    - go/pkg/traits/traitset.go (Task 1) — cache fields to populate
    - .planning/phases/02-trait-type-system/02-PATTERNS.md — "resolve.go" section (CAP=100 clamp, D-03)
  </read_first>
  <action>
    Create `go/pkg/traits/resolve.go` in `package traits`. Define the package const `CAP = 100` (D-03: per-axis magnitude clamp) and a `clamp(v int) int` helper that pins to `[-CAP, +CAP]`. Define a per-stat modifier cap constant at your discretion (Claude's Discretion — pick a sane bound, e.g. `ModCap = 25`, and document the choice); clamp each stat's summed delta to `[-ModCap, +ModCap]`.

    Implement `func (ts *TraitSet) Resolve()`:
    - Initialize `risSum` (a fresh `map[types.DamageType]int`) and zero `modSum`/`caps` so re-resolving is idempotent.
    - Per-axis RIS sum into `risSum[axis]`: ADD each `Resistance.Magnitude` and each `Immunity.Magnitude` as positive contributions, SUBTRACT/ADD `Vulnerability.Magnitude` as the negative direction (D-01: positive sum trends toward Resist/Immune, negative toward Vuln). Decide and document the sign convention so it matches the threshold mapping the query layer uses (Task 3). After summing each axis, apply `clamp`.
    - Sum `Modifiers` per `Stat` index into `modSum[stat]`, then clamp each entry to the per-stat cap.
    - For each `Capability`, call `internCapability(Key)`; on ok, `ts.caps.Set(bit)`; on overflow (false) skip without panic (bounded-growth defensive design carried forward to P3 TOML input).
    - Set `resolved = true`. Iterate slices in stored order (deterministic). Provide a private accessor or keep risSum/modSum readable by the query methods in the same package.
  </action>
  <verify>
    <automated>cd go && go test ./pkg/traits/ -run 'TestResolve' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go/pkg/traits/resolve.go` declares `const CAP = 100` and a documented per-stat modifier cap
    - `func (ts *TraitSet) Resolve()` sums RIS magnitude per `types.DamageType`, clamps each axis to `[-CAP, +CAP]`, sums stat modifiers per stat (clamped), and ORs interned Capability bits into `caps`
    - A test proves two +60 Fire vulnerabilities clamp at the cap boundary (no stacking blowup — TRAIT-02)
    - Resolve is idempotent: calling it twice yields identical caps/risSum/modSum (proven by test)
    - Capability overflow (>256) during Resolve is skipped without panic
    - `cd go && go test ./pkg/traits/ -run TestResolve` exits 0
  </acceptance_criteria>
  <done>Resolve() produces clamped per-axis RIS sums, clamped per-stat mod sums, and the OR'd capability bitset; idempotent; tested for additive-cap behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Query API + tri-state ResolveImmunity + zero-alloc HasCapability (query.go, tests)</name>
  <files>go/pkg/traits/query.go, go/pkg/traits/query_test.go</files>
  <behavior>
    - ResolveImmunity(axis): summed >= +CAP -> Immune; +1..+CAP-1 -> Resist; sum < 0 -> Vuln; sum == 0 -> Normal (D-03 thresholds; precedence Immune > Resist > Vuln > Normal)
    - HasCapability("flight") is true after Resolve when a Capability{Key:"flight"} was present, false otherwise
    - HasCapability runs with zero heap allocations per call (testing.AllocsPerRun == 0) — SC#4
    - GetModifier(StatStr) returns the clamped summed delta; HasTrait(KindHook) is true iff Hooks is non-empty
    - HooksFor(OnDeath) returns only OnDeath hooks, in deterministic source order
  </behavior>
  <read_first>
    - go/pkg/combat/combat.go:308-373 — ImmunityResult enum values/order + precedence to mirror exactly (do NOT import combat)
    - go/pkg/types/flags.go:384-397 — `Has` value-receiver read-method style
    - go/pkg/types/flags_test.go:1-53 — white-box subtest + t.Errorf style, NO testify
    - go/pkg/traits/resolve.go (Task 2) — risSum/modSum/caps the query reads
    - go/pkg/traits/capability.go (Plan 01) — lookupCapability (non-allocating) for the zero-alloc path
    - .planning/phases/02-trait-type-system/02-PATTERNS.md — "query.go" section (threshold table + parity note)
  </read_first>
  <action>
    Create `go/pkg/traits/query.go` in `package traits`. Mirror the combat tri-state vocabulary WITHOUT importing `pkg/combat` (cycle risk, D-07): define `type ImmunityResult int` with iota constants `ImmNormal, ImmImmune, ImmResistant, ImmVulnerable` in the SAME order as `combat.ImmunityResult` so the P7 wiring is a direct translation, plus a `String()` method. Add a doc comment noting the precedence Immune > Resist > Vuln > Normal and that the eventual combat semantics are resist=2/3, vuln=3/2, immune=0 damage (verification deferred to P7/P8, not this phase).

    Implement five read-only methods on `*TraitSet` (TRAIT-03), all assuming `Resolve()` has run (document this precondition; optionally lazy-resolve if `!resolved`):
    - `func (ts *TraitSet) HasTrait(kind TraitKind) bool` — returns whether the matching per-kind slice is non-empty (switch on TraitKind).
    - `func (ts *TraitSet) HasCapability(key string) bool` — call `lookupCapability(key)`; if registered, return `ts.caps.Has(bit)`. MUST be O(1) and zero-alloc: do not build strings, do not range slices, do not allocate maps in this path (SC#4).
    - `func (ts *TraitSet) GetModifier(stat types.Stat) int` — return `ts.modSum[stat]` (bounds-guard the index).
    - `func (ts *TraitSet) ResolveImmunity(axis types.DamageType) ImmunityResult` — read the clamped `risSum[axis]` and map per D-03 thresholds above.
    - `func (ts *TraitSet) HooksFor(event HookEvent) []BehaviorHook` — filter `ts.Hooks` by `Event == event`, preserving stored (source) order.

    Create `go/pkg/traits/query_test.go` (white-box `package traits`, stdlib `testing` only, NO testify). Cover: ResolveImmunity at each threshold boundary (sum == +CAP -> Immune; sum == +CAP-1 / small positive -> Resist; sum == -1 / negative -> Vuln; sum == 0 -> Normal); HasCapability present/absent; GetModifier summed value; HasTrait for a present and an absent kind; HooksFor returns only matching events in source order. Add the SC#4 allocation test: register/resolve a capability, then assert `testing.AllocsPerRun(1000, func(){ _ = ts.HasCapability("flight") }) == 0`. Use `t.Run` subtests and `t.Errorf("ResolveImmunity(%v) = %v, expected %v", ...)` messages.
  </action>
  <verify>
    <automated>cd go && go test ./pkg/traits/ -v && go vet ./pkg/traits/</automated>
  </verify>
  <acceptance_criteria>
    - `go/pkg/traits/query.go` defines `type ImmunityResult int` with constants `ImmNormal, ImmImmune, ImmResistant, ImmVulnerable` in that order (mirrors combat without importing it)
    - All five methods exist with signatures: `HasTrait(TraitKind) bool`, `HasCapability(string) bool`, `GetModifier(types.Stat) int`, `ResolveImmunity(types.DamageType) ImmunityResult`, `HooksFor(HookEvent) []BehaviorHook`
    - `go/pkg/traits/query_test.go` asserts `testing.AllocsPerRun(1000, func(){ _ = ts.HasCapability(...) }) == 0` (SC#4 explicit zero-alloc test)
    - ResolveImmunity threshold tests cover all four outputs (Immune/Resist/Vuln/Normal) at the D-03 boundaries
    - `pkg/combat` is NOT imported anywhere in `go/pkg/traits/` (verify: `cd go && ! grep -rq '"rotmud/pkg/combat"' pkg/traits/`)
    - `cd go && go test ./pkg/traits/` exits 0; `go vet ./pkg/traits/` exits 0
  </acceptance_criteria>
  <done>Five-method query API callable from any package (TRAIT-03); tri-state ResolveImmunity mirrors combat precedence without importing it; HasCapability proven zero-alloc (SC#4); full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none this phase) | Pure-Go in-memory composition + query API. No network I/O, no user input, no persistence. `Resolve()` interns only trusted in-process capability strings this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | per-axis RIS sum (`Resolve`) | mitigate | Per-axis `[-CAP, +CAP]` clamp (D-03) prevents additive stacking blowup — bounded magnitude regardless of source count (TRAIT-02). Covered by the +60+60 Fire cap-boundary test. |
| T-02-03 | Denial of Service | capability interning in `Resolve` | accept | The 256-bit ceiling (enforced in Plan 01's registry) bounds growth; Resolve skips overflow without panic. Forward-looking note: P3 feeds TOML-sourced strings into this path. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (pure stdlib + existing `rotmud/pkg/types`). No supply-chain surface. |

Attack surface is otherwise N/A: no untrusted input crosses a boundary in Phase 2. No high-severity threats to block on.
</threat_model>

<verification>
- `cd go && go test ./pkg/traits/` exits 0 (all subtests green, incl. zero-alloc HasCapability)
- `cd go && go vet ./pkg/traits/` exits 0
- `cd go && ! grep -rq '"rotmud/pkg/combat"' pkg/traits/` — pkg/combat is NOT imported (no future cycle, D-07)
- `git status` shows only new files under `go/pkg/traits/` — zero edits to `Character`, `pkg/combat`, `pkg/magic`, `races.go`, `classes.go`, or `pkg/golden/` (D-07 scope fence)
- `go test ./...` from `go/` still passes (no existing test broken; golden-master untouched)
</verification>

<success_criteria>
- Additive composition with per-axis caps (TRAIT-02): Compose/Merge concatenate deterministically; Resolve clamps each axis to `[-CAP, +CAP]` and each stat to the per-stat cap
- Query API callable from any package and unit-tested (TRAIT-03): HasTrait, HasCapability, GetModifier, ResolveImmunity, HooksFor
- ResolveImmunity reproduces the tri-state vocabulary + precedence of `combat.ImmunityResult` without importing `pkg/combat`
- SC#4: HasCapability is O(1) zero-allocation, proven by `testing.AllocsPerRun == 0`
- Zero edits to existing files (D-07)
</success_criteria>

<output>
Create `.planning/phases/02-trait-type-system/02-02-SUMMARY.md` when done
</output>
